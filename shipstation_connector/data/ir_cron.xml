<odoo>
    <record id="ir_cron_shipstation_sync_orders" model="ir.cron">
        <field name="name">ShipStation Sync Orders</field>
        <field name="model_id" ref="model_shipstation_instance"/>
        <field name="state">code</field>
        <field name="code">model.run_cron_sync_orders()</field>
        <field name="interval_number">10</field>
        <field name="interval_type">minutes</field>
        <field name="active">True</field>
    </record>

    <record id="ir_cron_shipstation_sync_shipments" model="ir.cron">
        <field name="name">ShipStation Sync Shipments</field>
        <field name="model_id" ref="model_shipstation_instance"/>
        <field name="state">code</field>
        <field name="code">model.run_cron_sync_shipments()</field>
        <field name="interval_number">15</field>
        <field name="interval_type">minutes</field>
        <field name="active">True</field>
    </record>

    <record id="ir_cron_shipstation_sync_products" model="ir.cron">
        <field name="name">ShipStation Sync Products</field>
        <field name="model_id" ref="model_shipstation_instance"/>
        <field name="state">code</field>
        <field name="code">model.run_cron_sync_products()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="active">True</field>
    </record>

    <record id="ir_cron_shipstation_sync_customers" model="ir.cron">
        <field name="name">ShipStation Sync Customers</field>
        <field name="model_id" ref="model_shipstation_instance"/>
        <field name="state">code</field>
        <field name="code">model.run_cron_sync_customers()</field>
        <field name="interval_number">30</field>
        <field name="interval_type">minutes</field>
        <field name="active">False</field>
    </record>

    <record id="ir_cron_shipstation_sync_inventory" model="ir.cron">
        <field name="name">ShipStation Sync Inventory</field>
        <field name="model_id" ref="model_shipstation_instance"/>
        <field name="state">code</field>
        <field name="code">model.run_cron_sync_inventory()</field>
        <field name="interval_number">60</field>
        <field name="interval_type">minutes</field>
        <field name="active">False</field>
    </record>
</odoo>
